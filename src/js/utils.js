//--------------------------------------------------
//utilidadaes 
//--------------------------------------------------


//scrol hacia arriba de cuando se carga la pagina sigiente o anterior
export function scrollUp() {
  window.scrollTo({
    top: 0,
    behavior: "smooth",
  });
}
